{
  "id": "agent-provisioner",
  "name": "Agent Provisioner",
  "description": "Expose an external HTTP endpoint that creates or updates OpenClaw agents.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "path": {
        "type": "string"
      },
      "authToken": {
        "type": "string"
      }
    }
  },
  "uiHints": {
    "path": {
      "label": "HTTP Path",
      "help": "Plugin route path exposed by the gateway, for example /plugins/agent-provisioner."
    },
    "authToken": {
      "label": "Auth Token",
      "help": "Bearer token required by the provisioning endpoint. Can also be set with OPENCLAW_AGENT_PROVISIONER_TOKEN."
    }
  }
}
